{
    "files":{
       "sources": [
            { "from":"sources", "to":"." }
       ],
       "templates": [
            "configs/{{ terraform_sage_environment }}/{{ variables }}"
       ],
       "directories": [
            "configs/{{ terraform_sage_environment }}"
       ],
       "aliases": {
            "variables":"templates/variables.tfvars"
       }
    },
    "variables": {
       "terraform_sage_environment": [
           "dev", 
           "production", 
           "staging"
       ]
    }
 }
